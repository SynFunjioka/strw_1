enum tVideo { guitar, piano, violin }
export default interface iVideo{
    id      : number,
    type    : string,
    name    : string
    path    : string
    image   : string
    duration: string
}